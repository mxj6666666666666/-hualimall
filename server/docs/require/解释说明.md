好的，我现在以**Java架构师**的身份，为你规划这个**能让外包公司认可的商城项目**。

我会按照企业级项目的标准来规划，但同时考虑到你是大学生、使用AI辅助开发的实际情况，做到**专业但不复杂，完整但不过度**。

---

## 一、项目总体架构设计

### 1.1 技术栈选型（企业级但轻量）

```
┌─────────────────────────────────────────────┐
│              前端层（Thymeleaf）              │
├─────────────────────────────────────────────┤
│              Controller层                    │
├─────────────────────────────────────────────┤
│   Service层   │    Cache层（Redis可选）      │
├─────────────────────────────────────────────┤
│              Mapper层（MyBatis-Plus）        │
├─────────────────────────────────────────────┤
│              MySQL数据库                      │
└─────────────────────────────────────────────┘
```

**详细选型清单：**

| 组件     | 技术          | 版本   | 选型理由                        |
| -------- | ------------- | ------ | ------------------------------- |
| JDK      | Java 25       | 11.0.x | 稳定、主流、Spring Boot完美支持 |
| 框架     | Spring Boot   | 4.0.5  | 最后一个稳定版本，资料丰富      |
| ORM      | MyBatis-Plus  | 3.5.5  | 简化CRUD，AI生成效率高          |
| 数据库   | MySQL         | 8.0    | 主流，提供初始化脚本即可        |
| 连接池   | HikariCP      | 自带   | Spring Boot默认，性能优秀       |
| 权限     | JWT           | 0.11.5 | 无状态，适合前后端分离          |
| 接口文档 | Knife4j       | 4.1.0  | Swagger增强版，界面更友好       |
| 工具库   | Hutool        | 5.8.25 | 减少重复代码，AI很熟悉          |
| 缓存     | Redis（可选） | 7.0    | 做购物车缓存，加分项            |
| 构建     | Maven         | 3.8+   | 标准，不要用Gradle              |

### 1.2 项目模块划分

```
mall-demo/
├── mall-common/          # 通用模块
│   ├── dto/              # 数据传输对象
│   ├── vo/               # 视图对象
│   ├── constant/         # 常量定义
│   └── exception/        # 自定义异常
├── mall-framework/       # 框架配置
│   ├── config/           # Spring配置
│   ├── handler/          # 处理器（全局异常、统一返回）
│   └── interceptor/      # 拦截器（JWT验证）
└── mall-business/        # 业务模块
    ├── user/             # 用户模块
    ├── product/          # 商品模块
    ├── cart/             # 购物车模块
    └── order/            # 订单模块
```

**简化版**（推荐你做这个）：不用分多个Maven模块，一个项目用包区分即可：

```
com.demo.mall/
├── controller/    # 接口层
├── service/       # 业务层
│   └── impl/      # 业务实现
├── mapper/        # 数据层
├── entity/        # 实体类
├── dto/           # 请求/响应对象
├── config/        # 配置类
├── common/        # 通用类
│   ├── Result.java       # 统一返回
│   ├── ResultCode.java   # 状态码
│   └── BizException.java # 业务异常
├── interceptor/   # 拦截器
└── utils/         # 工具类
```

---

## 二、数据库设计（5张核心表）

### 2.1 ER图关系

```
user (用户) ──┬── cart (购物车)
             │
             └── order (订单) ──┬── order_item (订单商品)
                               │
product (商品) ────────────────┘
```

### 2.2 建表语句（可直接给AI生成）

我给你标准结构，你可以让AI根据这个生成SQL：

**用户表 (user)**
```sql
CREATE TABLE `user` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `username` varchar(50) NOT NULL COMMENT '用户名',
  `password` varchar(255) NOT NULL COMMENT '密码(BCrypt加密)',
  `nickname` varchar(50) COMMENT '昵称',
  `phone` varchar(20) COMMENT '手机号',
  `role` varchar(20) DEFAULT 'USER' COMMENT '角色: USER/ADMIN',
  `status` tinyint DEFAULT 1 COMMENT '状态: 0禁用 1启用',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY `uk_username` (`username`)
);
```

**商品表 (product)**
```sql
CREATE TABLE `product` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `name` varchar(200) NOT NULL COMMENT '商品名称',
  `category_id` bigint COMMENT '分类ID',
  `price` decimal(10,2) NOT NULL COMMENT '价格',
  `stock` int NOT NULL DEFAULT 0 COMMENT '库存',
  `image_url` varchar(500) COMMENT '图片URL',
  `description` text COMMENT '商品描述',
  `status` tinyint DEFAULT 1 COMMENT '状态: 0下架 1上架',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

**商品分类表 (category)** - 可选但推荐
```sql
CREATE TABLE `category` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `name` varchar(50) NOT NULL,
  `parent_id` bigint DEFAULT 0,
  `sort_order` int DEFAULT 0
);
```

**购物车表 (cart)**
```sql
CREATE TABLE `cart` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `product_id` bigint NOT NULL,
  `quantity` int NOT NULL DEFAULT 1,
  `selected` tinyint DEFAULT 1 COMMENT '是否选中',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY `uk_user_product` (`user_id`, `product_id`)
);
```

**订单表 (order)**
```sql
CREATE TABLE `order` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `order_no` varchar(32) NOT NULL COMMENT '订单号',
  `user_id` bigint NOT NULL,
  `total_amount` decimal(10,2) NOT NULL COMMENT '总金额',
  `status` tinyint DEFAULT 0 COMMENT '0待支付 1待发货 2已发货 3已完成 4已取消',
  `receiver_name` varchar(50) COMMENT '收货人',
  `receiver_phone` varchar(20) COMMENT '收货电话',
  `receiver_address` varchar(200) COMMENT '收货地址',
  `remark` varchar(500) COMMENT '备注',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY `uk_order_no` (`order_no`),
  KEY `idx_user_id` (`user_id`)
);
```

**订单商品表 (order_item)**
```sql
CREATE TABLE `order_item` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `order_id` bigint NOT NULL,
  `product_id` bigint NOT NULL,
  `product_name` varchar(200) NOT NULL,
  `product_image` varchar(500),
  `price` decimal(10,2) NOT NULL,
  `quantity` int NOT NULL,
  `total_amount` decimal(10,2) NOT NULL,
  KEY `idx_order_id` (`order_id`)
);
```

---

## 三、接口设计（RESTful API）

### 3.1 接口清单（15个核心接口）

| 模块       | 方法   | 路径                       | 说明                      |
| ---------- | ------ | -------------------------- | ------------------------- |
| **用户**   | POST   | `/api/user/register`       | 注册                      |
|            | POST   | `/api/user/login`          | 登录（返回token）         |
|            | GET    | `/api/user/info`           | 获取个人信息（需token）   |
| **商品**   | GET    | `/api/product/list`        | 商品列表（分页+分类筛选） |
|            | GET    | `/api/product/{id}`        | 商品详情                  |
|            | GET    | `/api/category/list`       | 分类列表                  |
| **购物车** | GET    | `/api/cart/list`           | 购物车列表                |
|            | POST   | `/api/cart/add`            | 添加到购物车              |
|            | PUT    | `/api/cart/update`         | 修改数量                  |
|            | DELETE | `/api/cart/remove/{id}`    | 删除购物车项              |
| **订单**   | POST   | `/api/order/create`        | 创建订单                  |
|            | GET    | `/api/order/list`          | 订单列表                  |
|            | GET    | `/api/order/{id}`          | 订单详情                  |
|            | PUT    | `/api/order/cancel/{id}`   | 取消订单                  |
| **管理端** | POST   | `/admin/product`           | 添加商品                  |
|            | PUT    | `/admin/product/{id}`      | 修改商品                  |
|            | DELETE | `/admin/product/{id}`      | 删除商品                  |
|            | PUT    | `/admin/order/{id}/status` | 修改订单状态              |

### 3.2 统一返回格式（所有接口一致）

```java
public class Result<T> {
    private Integer code;      // 200成功，其他失败
    private String message;    // 提示信息
    private T data;            // 返回数据
    private Long timestamp;    // 时间戳
    
    // 快捷方法
    public static <T> Result<T> success(T data) {
        return new Result<>(200, "success", data);
    }
    
    public static <T> Result<T> error(String message) {
        return new Result<>(500, message, null);
    }
}
```

### 3.3 状态码规范

| 状态码 | 说明             |
| ------ | ---------------- |
| 200    | 成功             |
| 400    | 参数错误         |
| 401    | 未登录/token失效 |
| 403    | 无权限           |
| 404    | 资源不存在       |
| 500    | 服务器错误       |

---

## 四、核心业务逻辑（需要你手写理解的部分）

### 4.1 下单流程（最重要，必须自己理解）

```
用户点击下单
    ↓
1. 查询购物车选中的商品
    ↓
2. 检查库存（逐个商品）
    ↓ 库存不足 → 返回"xxx商品库存不足"
    ↓
3. 生成订单号（时间戳+随机数）
    ↓
4. 扣减库存（批量更新）
    ↓
5. 创建订单记录
    ↓
6. 创建订单商品记录
    ↓
7. 清空购物车（选中的商品）
    ↓
8. 返回订单信息
```

**关键点**：第2-4步必须在一个数据库事务中，AI生成的代码可能漏掉`@Transactional`。

### 4.2 JWT认证流程

```
登录 → 生成token → 返回给前端
    ↓
后续请求在Header中携带: Authorization: Bearer <token>
    ↓
拦截器验证token → 解析用户信息 → 存入ThreadLocal
    ↓
Controller中通过工具类获取当前用户ID
```

---

## 五、AI生成代码的详细提示词（按模块）

### 5.1 项目初始化

```
我是一个Java开发者，请帮我生成一个Spring Boot 2.7.18项目的pom.xml，包含以下依赖：
- spring-boot-starter-web
- mybatis-plus-boot-starter 3.5.5
- mysql-connector-java
- jwt (jjwt 0.11.5)
- knife4j-spring-boot-starter 4.1.0
- hutool-all 5.8.25
- spring-boot-starter-test
```

### 5.2 实体类和Mapper

```
请基于下面的表结构生成MyBatis-Plus的实体类和Mapper：

[粘贴上面User表的建表语句]

要求：
- 使用@TableName注解指定表名
- 使用@TableId指定主键策略为IdType.AUTO
- 使用@TableField自动映射字段
- Mapper继承BaseMapper
```

### 5.3 用户登录注册

```
请生成用户登录注册的Service和Controller：

要求：
1. 注册：用户名唯一，密码使用BCryptPasswordEncoder加密
2. 登录：验证用户名密码，成功后生成JWT token
3. JWT使用jjwt，有效期7天
4. 返回统一格式Result
```

### 5.4 下单功能（手写重点）

```
请生成创建订单的Service方法：

参数：用户ID（从token获取），收货地址信息

业务逻辑：
1. 查询用户选中的购物车商品
2. 检查每个商品的库存（product表）
3. 库存不足时抛出异常
4. 扣减库存
5. 生成订单号（格式：ORD + yyyyMMddHHmmss + 4位随机数）
6. 保存订单和订单商品
7. 清空购物车中选中的商品

要求：
- 使用@Transactional事务
- 返回订单详情
```

---

## 六、项目配置文件

### application.yml（完整配置）

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mall_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5

  redis:  # 可选，购物车缓存用
    host: localhost
    port: 6379
    database: 0

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # 开发时打印SQL
    map-underscore-to-camel-case: true

jwt:
  secret: your-secret-key-must-be-at-least-256-bits-long
  expiration: 604800000  # 7天，单位毫秒

knife4j:
  enable: true
  production: false
```

---

## 七、交付清单（让外包公司觉得专业）

### 必须包含的文件

```
mall-demo/
├── README.md                 # 详细说明（模板见下）
├── pom.xml
├── src/                      # 源代码
├── sql/
│   ├── schema.sql           # 建表语句
│   └── data.sql             # 初始化数据（10+商品）
├── docs/
│   ├── 接口文档.md           # 或直接提供Swagger地址
│   └── 部署文档.md
└── demo.gif 或 演示视频.mp4
```

### README模板

```markdown
# 电商商城系统（演示版）

## 项目简介
基于Spring Boot + MyBatis-Plus的轻量级商城系统，包含商品浏览、购物车、下单、订单管理等核心功能。

## 技术栈
- Spring Boot 2.7.18
- MyBatis-Plus 3.5.5
- MySQL 8.0
- JWT（登录认证）
- Knife4j（接口文档）

## 快速启动（3分钟）

### 环境要求
- JDK 11+
- MySQL 8.0
- Maven 3.6+

### 步骤
1. 创建数据库：`CREATE DATABASE mall_db;`
2. 执行sql/schema.sql和sql/data.sql
3. 修改application.yml中的数据库密码
4. 运行MallApplication.java
5. 访问：http://localhost:8080

## 测试账号
| 角色 | 用户名 | 密码 |
|------|--------|------|
| 普通用户 | user | 123456 |
| 管理员 | admin | 123456 |

## 接口文档
启动后访问：http://localhost:8080/doc.html

## 功能演示
![演示](./demo.gif)

## 项目结构
[项目目录树]

## 注意事项
- 支付功能为模拟支付，正式使用时需接入真实支付接口
- 当前使用内存/文件存储，生产环境建议配置Redis
```

---

## 八、开发排期（3周，每天2-3小时）

| 阶段      | 天数   | 任务                                 | AI使用方式                   |
| --------- | ------ | ------------------------------------ | ---------------------------- |
| **第1周** | Day1   | 项目初始化，pom.xml，application.yml | 让AI生成                     |
|           | Day2   | 建表，生成实体类和Mapper             | 让AI生成                     |
|           | Day3   | 用户注册登录（JWT）                  | 让AI生成，理解JWT流程        |
|           | Day4   | 商品列表、详情、分类                 | 让AI生成                     |
|           | Day5-6 | **购物车模块**                       | 让AI生成，测试               |
|           | Day7   | 前端基础页面（Thymeleaf）            | 让AI生成                     |
| **第2周** | Day1-2 | **下单模块（核心，手写理解）**       | AI生成骨架，自己写事务和库存 |
|           | Day3-4 | 订单列表、订单详情、取消订单         | 让AI生成                     |
|           | Day5-6 | 管理后台（商品CRUD）                 | 让AI生成                     |
|           | Day7   | 管理后台（订单状态修改）             | 让AI生成                     |
| **第3周** | Day1-2 | 统一异常处理、全局拦截器             | 让AI生成，自己调整           |
|           | Day3   | Knife4j接口文档配置                  | 让AI生成                     |
|           | Day4   | 写README、录演示视频                 | 自己做                       |
|           | Day5-6 | 测试、修复bug                        | 自己做                       |
|           | Day7   | 整理GitHub，准备作品集               | 自己做                       |

---

## 九、最后的话

作为你的架构师，我给你三个建议：

### 1. 先跑通再优化
不要追求完美。先让所有接口能跑通，再回头优化代码。能用就行。

### 2. 关键代码必须理解
AI可以帮你写90%的代码，但**下单时的库存扣减、JWT的验证逻辑、事务的控制**这三块，你必须自己写一遍或者完全理解。面试/外包沟通时会问到。

### 3. 做完就是胜利
很多大学生项目做到一半就放弃了。你只要把这个项目完整做出来、放到GitHub上，就已经超过了90%的同龄人。

**现在，你可以开始了。** 把上面第5节的第一个提示词发给AI，生成pom.xml，然后一步步往下做。

如果在某个模块卡住了，随时来问我，我会帮你定位问题。