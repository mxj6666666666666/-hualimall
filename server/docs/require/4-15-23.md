● Task complete
└ 已继续完成并落地为 RESTful 风格后端实现，核心链路已经从“商品+订单骨架”扩展到“用户认证 + 购物车 + 订单 + 管理端状态更新”。

    主要变更：

     1. RESTful 路径升级（兼容旧接口）

     - 商品：GET /products（兼容 /products/list）
     - 订单：POST /orders、GET /orders、PUT /orders/{id}/cancel（兼容旧 /order/*）
     - 用户：POST /users、POST /users/sessions、GET /users/me（兼容旧 /user/*）
     - 购物车：/carts 全套 CRUD（兼容旧 /cart/*）

     1. 新增用户与鉴权模块

     - 新增 UserController/UserService/UserMapper 与 User、登录请求/响应对象。
     - 新增 JwtUtils、AuthInterceptor、WebMvcConfig、AuthContext。
     - 受保护接口支持 Authorization: Bearer <token>。

     1. 新增购物车模块并打通下单

     - 新增 CartController/CartService/CartMapper 与 CartItem、请求 DTO。
     - 下单逻辑支持：当未传 items 时，自动使用当前用户“已选购物车项”生成订单。
     - 下单后自动清理已选购物车项。

     1. 订单链路加固

     - createOrder 加 @Transactional。
     - 新增库存扣减 SQL（decreaseStock）并校验受影响行数，防止超卖。
     - 增加参数合法性校验与统一异常返回（GlobalExceptionHandler）。

     1. 商品查询与管理端增强

     - ProQueryParams 增加 categoryId/status/keyword。
     - ProductMapper.xml 启用动态筛选条件。
     - 新增管理端订单状态更新：PUT /admin/orders/{id}/status。

     1. 交付文件补齐

     - 新增 README.md（接口与启动说明）。
     - 新增 docs/schema.sql、docs/data.sql。

    补充说明：

     - 所有计划 todo 已更新为 done。
     - mvn test 在当前环境受 Maven 镜像拦截（Blocked mirror for repositories）导致依赖下载失败，